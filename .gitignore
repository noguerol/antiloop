# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Local pi project config
.pi/
# t-plan: plan files are private runtime state — never commit or publish them
plan_*_[0-9a-zA-Z]*.md
plan.md

# punched-memory — never commit project-local memory
pi.md
pi.md.local
.punched-memory.json
.punched-memory/
